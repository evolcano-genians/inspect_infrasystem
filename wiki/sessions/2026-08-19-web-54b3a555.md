---
date: '2026-08-19'
session_id: web-54b3a555
type: session
---

# 세션 web-54b3a555 (2026-08-19)

## 질문

전체 적으로 권한 확인해줘

## 결론

현재 관찰 기준으로 Azure 사용자 `nexus`는 `nexus-shell` 네임스페이스의 일반 조회 권한은 대부분 정상입니다.  
AWS와 비교해 Azure에서 명확히 빠진 권한은 **코어 Node `get/list`**, **CRD 정의 `list`**, **Traefik `IngressRoute` 인스턴스 `list`**입니다.

**권한 비교**

| 범위 | 리소스·동작 | AWS | Azure |
|---|---|---:|---:|
| 클러스터 | `namespaces/list` | 허용 | 허용 |
| 클러스터 | `nodes/list` | 허용 | **거부** |
| 클러스터 | `nodes/get` | 확인 필요 | **거부** |
| 클러스터 | `customresourcedefinitions/list` | 허용 | **거부** |
| Metrics API | 노드 사용량 조회 | 확인 필요 | 허용 |
| `nexus-shell` | Pods `list/get/log` | `list` 허용 | 허용 |
| `nexus-shell` | Deployments `list/get` | `list` 허용 | 허용 |
| `nexus-shell` | ReplicaSet 롤아웃 이력 조회 | 확인 필요 | 허용 |
| `nexus-shell` | StatefulSets/DaemonSets `list` | 허용 | 허용 |
| `nexus-shell` | Jobs/CronJobs `list` | 허용 | 허용 |
| `nexus-shell` | Services `list/get`, endpoints 조회 | `list` 허용 | 허용 |
| `nexus-shell` | [REDACTED] `list` | 허용 | 허용 |
| `nexus-shell` | Pod Metrics 조회 | 확인 필요 | 허용 |
| `nexus-shell` | `traefik.io/ingressroutes/list` | 확인 필요 | **거부** |

Azure에서 보완할 최소 read-only 규칙은 다음과 같습니다.

```yaml
rules:
  - apiGroups: [""]
    resources: ["nodes"]
    verbs: ["get", "list"]
  - apiGroups: ["apiextensions.k8s.io"]
    resources: ["customresourcedefinitions"]
    verbs: ["get", "list"]
  - apiGroups: ["traefik.io"]
    resources: ["ingressroutes"]
    verbs: ["get", "list"]
```

- Node Metrics가 허용되는 이유는 `metrics.k8s.io/nodes`와 코어 API의 `/api/v1/nodes`가 별도 권한이기 때문입니다.
- 이번 namespaced 검사는 공통 플랫폼 네임스페이스인 `nexus-shell` 기준입니다. 네임스페이스마다 RoleBinding이 다르면 다른 결과가 나올 수 있습니다.
- 현재 도구에는 `SelfSubjectRulesReview` 또는 `kubectl auth can-i --list` 기능이 없어 RBAC 전체 규칙을 직접 열람한 것은 아니며, **실제 API 요청 성공/403 결과로 검증한 권한 목록**입니다.
- Secret과 변경 권한(`create/update/delete/patch`)은 안전 정책상 검사 범위 밖입니다.
- 관찰 시점: **2026-08-19 01:32 UTC 전후**. 저는 read-only이므로 권한을 직접 추가할 수 없습니다.

## 도구 호출 (audit 로그 상위 요약 — 원본은 logs/)

- `k8s_compare` → 허용
- `k8s_compare` → 허용
- `k8s_compare` → 허용
- `k8s_compare` → 허용
- `k8s_compare` → 허용
- `k8s_compare` → 허용
- `k8s_compare` → 허용
- `k8s_compare` → 허용
- `k8s_compare` → 허용
- `k8s_compare` → 허용
- `k8s_compare` → 허용
- `k8s_compare` → 허용
- `k8s_compare` → 허용
- `k8s_compare` → 허용
- `k8s_get_pod` → 허용
- `k8s_get_pod_logs` → 허용
- `k8s_get_deployment` → 허용
- `k8s_rollout_history` → 허용
- `k8s_get_service` → 허용
- `k8s_top_pods` → 허용
- `k8s_top_nodes` → 허용
- `k8s_cluster_info` → 허용
- `k8s_list_custom` → 허용
- `k8s_get_node` → 허용
