---
date: '2026-08-18'
session_id: dev-debug-1
type: session
---

# 세션 dev-debug-1 (2026-08-18)

## 질문

kube-system 네임스페이스에서 상태가 정상이 아니거나 재시작이 잦은 파드를 찾아서 원인을 요약해줘

## 결론

**결론:** 현재 `kube-system`에 장애 상태인 파드는 없습니다. 다만 오래된 Calico 파드 4개에서 누적 재시작이 71~76회로 높지만, 최근 수개월간은 재시작하지 않아 현재 진행 중인 재시작 루프로 보이지 않습니다.

- `calico-kube-controllers-5947598c79-kmwwn`: 76회, 현재 `Running/Ready`, 마지막 기동 2026-05-05
- `calico-node-s4dlx`: 75회, 현재 `Running/Ready`, 마지막 기동 2026-05-05
- `calico-node-td8dg`: 72회, 현재 `Running/Ready`, 마지막 기동 2026-06-24
- `calico-node-pvvsl`: 71회, 현재 `Running/Ready`, 마지막 기동 2026-05-05
- 신규 노드의 `calico-node-fk8pt`, `calico-node-tjqgn`은 재시작 0회입니다.

**원인 판단**
- 직전 컨테이너 로그에는 `OOMKilled`, 패닉, 치명적 오류가 없고 정상적인 Calico dataplane 처리 기록만 있습니다.
- 여러 노드의 Calico 구성요소가 2026-05-05 전후 함께 다시 시작된 점을 보면, 개별 애플리케이션 오류보다 당시 노드·컨테이너 런타임·클러스터 유지보수 같은 공통 인프라 재기동 가능성이 큽니다.
- 현재 Warning 이벤트는 0건이고 모든 노드 5대가 `Ready=True`입니다.
- `Pending`, `Failed`, `Unknown` 파드는 각각 0개입니다. `Succeeded` 상태의 `node-shell-*` 파드는 정상 완료된 일회성 파드이므로 장애로 분류하지 않았습니다.

관찰 시점은 **2026-08-18 기준**이며, 오래된 이벤트와 종료 상태가 이미 정리되어 정확한 과거 종료 원인은 현재 조회 자료만으로 확정할 수 없습니다.

## 도구 호출 (audit 로그 상위 요약 — 원본은 logs/)

- `k8s_list_pods` → 허용
- `k8s_list_pods` → 허용
- `k8s_list_pods` → 허용
- `k8s_get_pod` → 허용
- `k8s_get_pod` → 허용
- `k8s_get_pod` → 허용
- `k8s_get_pod` → 허용
- `k8s_get_pod_logs` → 허용
- `k8s_get_pod_logs` → 허용
- `k8s_get_pod_logs` → 허용
- `k8s_get_pod_logs` → 허용
- `k8s_list_events` → 허용
- `k8s_list_nodes` → 허용
- `k8s_list_pods` → 허용
- `k8s_list_pods` → 허용
- `k8s_list_pods` → 허용
