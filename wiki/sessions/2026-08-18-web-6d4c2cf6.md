---
date: '2026-08-18'
session_id: web-6d4c2cf6
type: session
---

# 세션 web-6d4c2cf6 (2026-08-18)

## 질문

발견한 리소스 문제 리포팅 해줘

## 결론

## AWS dev 리소스 문제 리포트

**결론:** 즉시 장애 상태는 아니지만, Silver 소비 구조에 **테이블 불일치·테넌트 고정·중복 데이터·대용량 조회 위험**이 확인됩니다. 특히 `security-view`의 scan report 테이블 매핑과 `ztna_devices` 중복 처리가 우선 점검 대상입니다.

| 심각도 | 문제 | 근거 및 영향 |
|---|---|---|
| 높음 | Scan report 테이블 불일치 | 2026-08-05 실측에서 `security-view` 설정은 `tatum_scan_reports`를 참조했지만 `delta.tenant_genians`에 해당 테이블이 없었습니다. 현재 ConfigMap에도 `SECURITY_TABLE_CLOUD_SCAN_REPORTS` 키가 남아 있어 관련 화면/API가 빈 결과 또는 쿼리 오류를 낼 가능성이 큽니다. |
| 높음 | 테넌트 스키마 하드코딩 | 2026-08-05 기준 앱이 로그인 tenant와 무관하게 `SECURITY_SCHEMA=tenant_genians`를 사용했습니다. 멀티테넌트 환경에서 잘못된 tenant 데이터 조회 또는 데이터 경계 위반 위험이 있습니다. |
| 중간 | NAC 데이터 논리 중복 | `ztna_devices` 94행은 폴링 데이터가 누적된 상태로, `upper(mac), ip` 기준 최신 행만 선택하는 dedup이 필요했습니다. 단순 `count(*)`나 자산 목록 조회는 실제 장비 수를 과대 계산합니다. |
| 중간 | 테이블 명명 체계 불일치 | 앱 설정은 `cloud_assets`, `cloud_entities`, `cloud_policies`, `nac_nodes`, `edr_threats` 같은 논리 이름을 사용하지만 실제 테이블은 `tatum_assets`, `tatum_entities`, `tatum_remediations`, `ztna_devices`, `edr114_90_xba`였습니다. 매핑 누락 시 즉시 조회 장애가 발생합니다. |
| 중간 | Bronze 대용량 직접조회 위험 | 2026-08-05 `bronze_raw_events`가 약 **440만 행**이었고, 현재도 최소 5개 raw topic이 계속 적재 중입니다. Trino 메모리 제약이 있어 topic/time 필터와 `LIMIT` 없는 조회는 OOM·지연 위험이 큽니다. |
| 중간 | Silver 전체 재작성 비용 | `silver_sync.py`는 promotion마다 Bronze를 읽고 Delta Silver를 `overwrite`합니다. 현재 append형 topic 4개 이상이 계속 누적 중이므로 데이터 증가에 따라 승격 시간과 S3 I/O가 지속 증가할 구조입니다. |
| 낮음 | Helm 외부 배포 | `security-view-backend`는 2026-08-05 당시 Helm에 통합되지 않은 out-of-band 배포였습니다. 현재 Pod는 정상 Running이지만 차트 재배포·클러스터 재구축 시 유실될 위험이 있습니다. |
| 낮음 | Bronze ingestor 재시작 이력 | 현재 `bronze-ingestor`는 Ready지만 재시작 횟수 1회입니다. 최근 로그에서는 적재가 계속되고 있어 현재 장애 증거는 아닙니다. |

**확인된 Silver 인벤토리 — 2026-08-05 실측**
- `tatum_assets`: 약 3,300행 — 클라우드/Kubernetes 자산
- `tatum_entities`: 116행 — IAM 사용자·권한·MFA 상태
- `tatum_remediations`: 91행 — CSPM 정책 위반·조치 대상
- `ztna_devices`: 94행 — NAC 장비 인벤토리, dedup 필요
- `edr_endpoints`: 1행 — EDR 엔드포인트
- `edr114_90_xba`: 1행 — EDR 탐지 이벤트, `detecttime`은 epoch-ms 문자열
- `ztna99_65_admin_activities`: 8행 — 관리자 활동 이력

현재 시점에는 Trino read-only SQL 도구가 없어 최신 행 수·컬럼·null 비율은 재검증하지 못했습니다. Kubernetes 기준으로는 `trino`, `hive-metastore`, `bronze-ingestor`, `registry-api`, `security-view-backend`가 모두 Ready 상태이며 즉시 장애 이벤트는 관찰되지 않았습니다.

## 도구 호출 (audit 로그 상위 요약 — 원본은 logs/)

- (도구 호출 없음 — 위키 재사용)
