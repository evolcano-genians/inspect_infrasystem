---
date: '2026-08-18'
session_id: web-23a9d9ca
type: session
---

# 세션 web-23a9d9ca (2026-08-18)

## 질문

현재 aws-seoul-clouddev 클러스터 read-only로 분석해서 kind에 띄어서 현재 pc에서 테스트 할 수 있는 환경 만들어줘

## 결론

**결론:** 현재 연결된 대상은 요청하신 `aws-seoul-clouddev`가 아니라 이미 kind로 구성된 `k8s-inspector-sandbox` 클러스터입니다. 또한 저는 **read-only 에이전트라 현재 PC에 kind 클러스터를 생성하거나 YAML 파일을 작성·적용할 수 없습니다.** 따라서 AWS 개발 클러스터를 동일하게 복제하는 작업은 현재 상태로는 진행할 수 없습니다.

- 관찰 시점: 클러스터 리소스 타임스탬프 기준 `2026-08-18 08:02 UTC` 전후
- Kubernetes `v1.36.1`, `linux/arm64`
- 노드 2개: control-plane 1개, worker 1개, 각각 CPU `12`, 메모리 약 `7.65Gi`
- 네임스페이스: `default`, `kube-system`, `local-path-storage` 및 기본 시스템 네임스페이스
- 정상 워크로드: `healthy-web` Deployment, `nginx:1.27-alpine`, replicas `2/2`, CPU `50m/200m`, 메모리 `64Mi/128Mi`
- 서비스: `healthy-web` ClusterIP, `80 -> 80`, 정상 엔드포인트 2개
- metrics-server가 없어 파드·노드 실시간 사용량 조회는 `404`로 불가능합니다.

**장애 Fixture**
- `crashloop-demo`: `CrashLoopBackOff`, 재시작 `15회`, BackOff 이벤트 `55회`. 현재/직전 로그 모두 의도적 crash 메시지로 동일해 반복 장애입니다. 제공된 상세 응답에는 exit code가 없어 확인되지 않았습니다.
- `imagepull-demo`: `ImagePullBackOff`, 재시작 `0회`, 이미지 `ghcr.io/inspect-k8s/does-not-exist:v1`. GHCR 인증 토큰 요청이 `403`이며 BackOff `240회`로 반복 패턴입니다. 컨테이너가 시작되지 않아 exit code와 로그가 없습니다.
- `high-resource-demo`: 정상 실행, 재시작 `0회`, CPU `500m/1`, 메모리 `256Mi/512Mi`.

**다음으로 확인할 것**
1. 조회 도구의 연결 대상을 실제 `aws-seoul-clouddev` 컨텍스트로 변경한 뒤 다시 요청해 주세요. 연결되면 네임스페이스별 Deployment·Pod·Service·ConfigMap·이미지·리소스 설정을 read-only로 재조사하겠습니다.
2. 실제 클러스터 연결 후에도 Secret 값은 조회 범위 밖이며, 현재 도구는 전체 원본 YAML과 컨테이너 command/args를 내보내지 않아 완전한 1:1 복제에는 별도의 권한 있는 `kubectl get ... -o yaml` 내보내기가 필요합니다.

## 도구 호출 (audit 로그 상위 요약 — 원본은 logs/)

- `k8s_ping` → 허용
- `k8s_cluster_info` → 허용
- `k8s_list_namespaces` → 허용
- `k8s_list_nodes` → 허용
- `k8s_list_pods` → 허용
- `k8s_list_deployments` → 허용
- `k8s_list_services` → 허용
- `k8s_list_configmaps` → 허용
- `k8s_list_pods` → 허용
- `k8s_list_deployments` → 허용
- `k8s_list_services` → 허용
- `k8s_list_pods` → 허용
- `k8s_get_pod` → 허용
- `k8s_list_events` → 허용
- `k8s_get_pod_logs` → 허용
- `k8s_get_pod_logs` → 허용
- `k8s_get_pod` → 허용
- `k8s_list_events` → 허용
- `k8s_get_pod_logs` → 허용
- `k8s_get_pod` → 허용
- `k8s_get_deployment` → 허용
- `k8s_get_service` → 허용
- `k8s_rollout_history` → 허용
- `k8s_top_pods` → 허용
- `k8s_top_nodes` → 허용
