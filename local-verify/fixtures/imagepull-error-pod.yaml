# 테스트 픽스처: ImagePullBackOff 상태를 재현하는 파드 (존재하지 않는 이미지)
apiVersion: v1
kind: Pod
metadata:
  name: imagepull-demo
  namespace: default
  labels:
    app: imagepull-demo
    fixture: "true"
spec:
  containers:
    - name: main
      image: ghcr.io/inspect-k8s/does-not-exist:v1
